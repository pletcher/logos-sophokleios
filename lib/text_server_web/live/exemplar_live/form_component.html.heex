<div>
  <h2><%= @title %></h2>

  <.form
    let={f}
    for={@changeset}
    id="exemplar-form"
    phx-target={@myself}
    phx-change="validate"
    phx-submit="save"
  >
    <%= label(f, :title) %>
    <%= text_input(f, :title) %>
    <%= error_tag(f, :title) %>

    <%= label(f, :slug) %>
    <%= text_input(f, :slug) %>
    <%= error_tag(f, :slug) %>

    <%= label(f, :description) %>
    <%= text_input(f, :description) %>
    <%= error_tag(f, :description) %>

    <%= label(f, :urn) %>
    <%= text_input(f, :urn) %>
    <%= error_tag(f, :urn) %>

    <div>
      <%= submit("Save", phx_disable_with: "Saving...") %>
    </div>
  </.form>
</div>
